package Cars;

public class Mercedes extends Car {
    public Mercedes(String model, int year, String color, boolean isAutomatic, double engineVolume) {
        super("Mercedes", model, year, color, isAutomatic, engineVolume);
    }
}
