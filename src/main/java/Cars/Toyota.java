package Cars;

public class Toyota extends Car{
    public Toyota(String model, int year, String color, boolean isAutomatic, double engineVolume) {
        super("Toyota", model, year, color, isAutomatic, engineVolume);
    }
}
