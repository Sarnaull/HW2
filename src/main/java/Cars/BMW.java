package Cars;

public class BMW extends Car {
    public BMW(String model, int year, String color, boolean isAutomatic, double engineVolume) {
        super("BMW", model, year, color, isAutomatic, engineVolume);
    }
}
